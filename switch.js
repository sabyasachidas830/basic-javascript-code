const action = "say_hello";
switch(action){
    case "say_hello":{
        const message = "Hii Traning Class    ";
        console.log(message);
        break;
    }
    case"say_hi":{
        const message="hi";//no error its a new block
        console.log(message);
        break;
    }
}